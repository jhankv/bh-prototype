# Observations — choice controls

**Phase 1: what is visible on screen.** Every entry here was seen in a running
frame and then measured in the DOM. None of it was read out of component source
— that pass comes later and lands in its own document, because an observation
and a code reading do not carry the same confidence and should never be mistaken
for each other.

Components on this surface: `toggle` · `checkbox` · `segmented-control` ·
`button-group`.

---

*No entries yet.*
